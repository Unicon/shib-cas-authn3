
repositories {
    mavenCentral()
    flatDir dirs: [getShibIdpPath() + '/lib']
}

dependencies {
    compile "javax.servlet:servlet-api:2.5"
    compile "edu.internet2.middleware:shibboleth-common:" + getShibCommonVersion()
    compile "edu.internet2.middleware:shibboleth-identityprovider:" + getShibIdpVersion()
}